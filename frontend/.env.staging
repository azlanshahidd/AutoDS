# Core Service frontend — STAGING
# Used when running the frontend dev server against the local staging stack.
# The staging container maps to port 4001 (see docker-compose.staging.yml).
VITE_API_BASE=http://127.0.0.1:4001
